{
  "name": "productivity-farm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/popup.html dist/popup.html && cp manifest.json dist/manifest.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.31",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
